float4 main(
		uniform float time,
		uniform float4 colorModulate) : COLOR
{
   float4 color;
   color.r = colorModulate.r;
   color.g = colorModulate.g;
   color.b = colorModulate.b;
   color.a = colorModulate.a * (abs(sin(time * 4.0)));
   return color;
}
